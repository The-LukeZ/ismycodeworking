{
  "name": "worker",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "format": "prettier --write .",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "typescript": "^5.5.2",
    "wrangler": "^4.33.1"
  }
}
